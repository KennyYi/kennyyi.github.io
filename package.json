{
  "name": "kennyyi.github.io",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "showcase": "npm run start --workspace=showcase",
    "deploy:showcase": "npm run deploy --workspace=showcase"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
